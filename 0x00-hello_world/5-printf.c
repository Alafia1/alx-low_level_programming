#include <stdio.h>

/**
 * main - prints using printf function
 * Return: 0 if exited properly; non-zero otherwise
 */
int main(void)
{
	char Mystr[];

	Mystr = "with proper grammar, but the outcome is a piece of art,";
	printf(Mystr);
	return (0);
}
