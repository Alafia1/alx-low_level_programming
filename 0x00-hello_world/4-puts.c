#include <stdio.h>

/**
 * main - prints th ephrase in the puts
 * Return: 0 if exited properly, non-zero otherwise
 */

int main(void)
{
	/**
	 * The main function is so cute
	 *
	 * Description: Printing the file with puts
	 *
	 */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
